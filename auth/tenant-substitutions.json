{
    "tenant": {
      "externalIdentifierConfiguration": {
      "issuer": "${ISSUER}",
      "jwtConfiguration": {
        "accessTokenKeyId": "${SIGNING_KEY_ID}",
      },
      "name": "${NAME}"
    }
  }
}